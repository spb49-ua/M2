%crea umna funcion en matlab que le pases una expresion, una variable, un
%punto y un lateral, y devuelve el limite
%otra que le pase los argumentos a la funcion
%derivada por limites(definicion de derivada)
%dibujar las asintotas de esta

function resultado = ejercicio1examen(expresion,var,punt,lateral)
resultado = limit(expresion,(var),punt,lateral);
end